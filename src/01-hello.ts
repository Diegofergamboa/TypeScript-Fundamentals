// On typescript we transpiled the code.
// Itś a good practice to create a file dist
// We can createwith this code "tsc /file --target es6 --outDist /distRout"
const myName = 'Diego'
console.log(myName)